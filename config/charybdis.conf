##################
##  Main Config ##
##################

## Bluetooth Stability & Connection Management
CONFIG_ZMK_BLE=y
CONFIG_ZMK_BLE_PASSKEY_ENTRY=y

# Boost signal strength for split-to-PC and split-to-split
CONFIG_BT_CTLR_TX_PWR_PLUS_8=y

# Disable problematic features for Windows/Intel/macOS chips
CONFIG_BT_GATT_ENFORCE_SUBSCRIPTION=n

# Relaxed intervals — aggressive polling overwhelms BLE with trackball data
CONFIG_BT_PERIPHERAL_PREF_MIN_INT=6
CONFIG_BT_PERIPHERAL_PREF_MAX_INT=12

# BLE buffers — more headroom for trackball event flood
CONFIG_BT_BUF_EVT_RX_COUNT=33
CONFIG_BT_BUF_ACL_TX_SIZE=69
CONFIG_BT_BUF_ACL_TX_COUNT=32
CONFIG_BT_L2CAP_TX_BUF_COUNT=32
CONFIG_BT_BUF_ACL_RX_SIZE=69


## Power Management 
# Sleep disabled to prevent disconnects on wake
CONFIG_ZMK_SLEEP=n
CONFIG_ZMK_IDLE_TIMEOUT=30000

## Battery & Macros
CONFIG_ZMK_BATTERY_REPORTING=y
CONFIG_ZMK_BEHAVIORS_QUEUE_SIZE=512

## Mouse & Performance
CONFIG_ZMK_POINTING=y
CONFIG_ZMK_KSCAN_DEBOUNCE_PRESS_MS=3
CONFIG_ZMK_KSCAN_DEBOUNCE_RELEASE_MS=7

## Stack sizes — Zephyr 4.1 BLE + PMW3610 needs more headroom
CONFIG_SYSTEM_WORKQUEUE_STACK_SIZE=12288
CONFIG_MAIN_STACK_SIZE=12288

